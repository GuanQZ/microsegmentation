apiVersion: v1
kind: Namespace
metadata:
  name: microsegmentation

---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: microsegmentation-sa
  namespace: microsegmentation

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: microsegmentation-role
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get","list","watch"]
  - apiGroups: ["apps"]
    resources: ["deployments"]
    verbs: ["get","list","watch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: microsegmentation-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: microsegmentation-role
subjects:
  - kind: ServiceAccount
    name: microsegmentation-sa
    namespace: microsegmentation

---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: microsegmentation
  namespace: microsegmentation
spec:
  selector:
    matchLabels:
      app: microsegmentation
  template:
    metadata:
      labels:
        app: microsegmentation
    spec:
      serviceAccountName: microsegmentation-sa
      hostNetwork: true
      tolerations:
        - operator: Exists
      containers:
        - name: microsegmentation
          image: __IMAGE_PLACEHOLDER__
          imagePullPolicy: IfNotPresent
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: API_BIND
              value: ":18080"
            # FORWARD 链跳转插入方式：insert（默认，优先生效）/ append（影响最小）
            - name: FORWARD_JUMP_POSITION
              value: "insert"
            # 可选：设置 API 访问令牌（客户端需带 X-API-Token）
            # - name: API_TOKEN
            #   value: "your-token"
            # 可选：策略持久化文件路径（为空则不落盘）
            # - name: POLICY_FILE
            #   value: "/var/lib/ms-iptables/policy.json"
          securityContext:
            capabilities:
              add: ["NET_ADMIN"]
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
          ports:
            - name: http
              containerPort: 18080
              protocol: TCP
          volumeMounts: []
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: microsegmentation-api
  namespace: microsegmentation
spec:
  selector:
    app: microsegmentation
  ports:
    - name: http
      port: 18080
      targetPort: 18080
      protocol: TCP

---
apiVersion: v1
kind: Service
metadata:
  name: microsegmentation-api-headless
  namespace: microsegmentation
spec:
  clusterIP: None
  selector:
    app: microsegmentation
  ports:
    - name: http
      port: 18080
      targetPort: 18080
      protocol: TCP
